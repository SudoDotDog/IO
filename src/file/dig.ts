/**
 * @author WMXPY
 * @namespace File
 * @description dig
 */

import { Folder } from "../model/folder";

export const digFolder = async (path: string): Promise<Folder> => {

    return new Folder();
};
